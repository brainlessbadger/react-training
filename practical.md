* refactor the form to use the trick from the presentation
* add a "select element" to choose mathematical operation to perform (+, -, *, / etc.)
    * add "validation" - disable the submit button to prevent division by 0